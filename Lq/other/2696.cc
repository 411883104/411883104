#include <iostream>
#include <set>
#include <vector>
using namespace std;

vector<set<int>> v;

int a[10005][3];

int main()
{
    int n, m, k;
    cin >> n >> m >> k;
    int x, y, z;
    for (int i = 1; i <= n; i++)
    {
        cin >> a[i][0] >> a[i][1] >> a[i][2];
    }

}