#include <iostream>
#include <algorithm>
#include <unordered_map>
using namespace std;

unordered_map<string, int> m;
int main()
{
    string s, r;
    cin >> s;
    int n;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> r;
        sort(r.begin(), r.end());
        
    }
}